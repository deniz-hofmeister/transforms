# v2 Execution Log

Companion to `v2-decision-record.md` (the authoritative stage plan). This file is updated by the
executing agent as each stage progresses, and is committed together with each stage. **Git history
on `feature/v2-audit-decisions` plus this table are the ground truth for resuming.**

## Resume protocol (cold start, any session)

1. `git log --oneline feature/v2-stable-gate..HEAD` and this table tell you what is done.
2. If the working tree is **dirty**, a stage was interrupted mid-flight: the row marked
   `IN PROGRESS` says which one. Inspect the diff against that stage's spec in
   `v2-decision-record.md`; either finish the stage, or `git reset --hard` to the last commit
   and redo it from scratch. Never salvage a half-understood diff — redoing a stage is cheap,
   a silently wrong merge is not.
3. Continue with the first row not marked `DONE`. Stages run strictly in order.
4. The full gate (`rustup run nightly tests/test_all.sh`) must be green before every stage commit.
5. Commits: imperative summary, why-body, trailer `Assisted-by: Claude:claude-fable-5`.
   Never push, tag, or publish from an execution session — the maintainer does that.

## Stage table

| Stage | Scope | Status | Commit(s) | Gate | Notes / deviations |
|---|---|---|---|---|---|
| 1 | Correctness semantics (D1, D3, D4) | DONE | a7e7cd3 + follow-up | GREEN | No source drift: the retain, the test, the `Stamp` derive and the contradicted rustdoc were all where the record said. `Buffer`'s pin-doc needed no edit — it already promised what D1 now makes true, so only the registry side was corrected. No CHANGELOG entry for D4: `Stamp` ships first in this unreleased rc.1, so the ordering derives never reached a user; the rationale is pinned as `Stamp` rustdoc and an AGENTS.md invariant instead. AGENTS.md gained two invariant clauses (pins survive cleanup; `Stamp` is deliberately unordered) — beyond the stage bullets, but required by the normative-docs rule. **Review follow-up:** both blockers verified real and are one defect — a7e7cd3 corrected `delete_transforms_before`'s own rustdoc but left `NotFoundAt`'s contract saying the frame "holds data" and carries "the covered range", which D1 falsified by making an empty-but-present buffer reachable (`NoTransformAvailable`, no range). Fixed at all five rustdoc sites (`transform/error.rs` variant doc, `Registry::get_transform` `# Errors`, `diagnose_not_found`, `process_get_transform`, `process_get_transform_at`); the docs now separate the two `BufferError` causes and state that the drained one is not a timing problem. Same false claim also fixed in MIGRATION.md item 3 (which told migrating users to size a retry window from `TimestampOutOfRange`) and behavior-change 5, in the AGENTS.md lookup invariant, and in the D1 CHANGELOG bullet. Doc-only: behavior already pinned by `delete_transforms_before_leaves_drained_frames_diagnosable`, so no new test. Stage 6 (D6) inherits corrected wording for `RegistryError`'s `covered: Option<(T, T)>`. |
| 2 | Time types (D11, D12) | DONE | f49c6f0 + follow-up | GREEN | No source drift: the trait, both impls, the adapter doctest and the proptest cap were all where the record said. Deviations: (a) `Timestamp::now()`'s panic wording is *not* kept verbatim — the u64 narrowing adds a second panic cause (a clock past 2554), and "time went backwards" would have misdiagnosed it, so the message and the rustdoc now name the representable range; AGENTS.md's panic invariant follows. (b) `Sub<Timestamp>` is simplified further than "drop the `seconds > u64::MAX` branch": under u64 nanos every difference is a valid `Duration`, so the whole `Ordering` match collapses to one `checked_sub().map(Duration::from_nanos)`, deleting the seconds/nanos split, both casts and their `cast_possible_truncation` allow. (c) `Add<Duration>`/`Sub<Duration>` were retyped too (not named in the bullet, but they carried the u128 arithmetic); a `Duration` wider than the u64 nanosecond range now reports overflow/underflow instead of being silently representable. (d) The inherent accessors keep `&self`: clippy's `trivially_copy_pass_by_ref` does not fire on `self` receivers at 8 bytes, so no signature churn was warranted. (e) The proptest strategy keeps the original sub-1e15 band and adds three (2^53 straddle, ~1.7e18 wall clock, top of u64), parameterized by a `headroom` the interpolation test uses so `start + span + outside` cannot overflow; band coverage was verified empirically (~25% each over 1000 draws) with a throwaway check that was then removed. Wire format confirmed unchanged as the record predicted — postcard goldens pass byte-identically (u64 varint ≡ u128 varint in range), so `tests/serde.rs` needed only a comment fix. Docs updated beyond the stage's named files where the change falsified them: README serde note (the `u128`/MessagePack-blob caveat is now false), core-types table and the `TimePoint` vs `Timestamp` section; `lib.rs` feature bullet; `time` module doc; AGENTS.md architecture + panic lines; MIGRATION items 5 and the `TimePoint` bullet; CHANGELOG, including deletion of the now-false "`checked_add` stays in the trait by decision" entry. **Review follow-up:** the two blockers filed are one defect, re-verified real — `try_now` grew a `DurationOverflow` arm and `now()` panics through it, so the crate-root Reliability bullet in `src/lib.rs` ("library code does not panic … the single documented exception is `Timestamp::now()` … before the Unix epoch") was falsified by this stage. The same contract was correctly restated in the other two places it appears (`Timestamp::now`'s rustdoc, AGENTS.md's panic invariant), so the crate shipped two contradictory panic contracts and the false one was the front-page rustdoc a docs.rs reader audits. Fixed by naming both causes in that bullet, in the wording already used by AGENTS.md and `try_now`'s `# Errors`. A sweep for the claim elsewhere (README, MIGRATION, CHANGELOG, rustdoc, the `SystemTime` impl) found no other stale site — CHANGELOG already recorded the second panic cause. Doc-only, so no new test: the overflow arm needs a system clock past 2554, which no deterministic fixture can produce. This makes the "docs updated beyond the stage's named files" list above complete — `lib.rs` needed the panic bullet as well as the feature bullet. **Gate repair (separate commit, unrelated to this stage):** the gate could not be run at all this session. `rustup run nightly` no longer selects rustup's pinned nightly (1.95, 2026-02-27) — a Nix `rust-minimal` 1.99.0-nightly-2026-08-10 precedes it in `PATH`, so `rustc`, `cargo` and `clippy` all resolve to it — and 1.99 hard-deprecates the `core::f64` module constants. `use core::f64;` in `src/geometry/quaternion/tests.rs` shadows the primitive, so 36 `f64::EPSILON`/`NAN`/`INFINITY` uses became errors under both `-D warnings` clippy steps. Fixed by dropping that import and spelling the six `f64::consts::PI` uses `core::f64::consts::PI`, as every other test file here already does — same constants, test-only, no behavior change. Pre-existing and not caused by Stage 2: it reproduces at `79a169e` with this stage's fix stashed. **For the maintainer:** the gate's "one pinned toolchain on every machine" premise does not hold on this box — rustup's nightly has no clippy component installed, so `cargo clippy` falls through to whatever toolchain `PATH` offers, and the gate silently ran on a compiler six months newer than the pinned one. |
| 3 | Numerics: libm everywhere (D10) | DONE | this commit | GREEN | No source drift: the `math` cfg fork was exactly where the record said, and `libm` was already non-optional. Deviations: (a) the `math` wrapper module is **deleted**, not kept with libm-only bodies — with the fork gone it was single-use indirection, so the five call sites now spell `libm::sqrt`/`sin`/`acos` and a file-level comment says why they are not `f64`'s. (b) The pinned tests take decimal-literal operands rather than computed rotations: an operand built with `.cos()`/`.sin()` would come from the *platform's* trig and make the pin platform-dependent, defeating its purpose. (c) Discrimination is uneven and worth knowing: only the **interior** pin fails against the pre-change `std` path (verified by temporarily restoring the intrinsics — two ulps in `w` and one in `x` on x86-64 glibc; the "one ulp per component" first recorded here was wrong, see the review follow-up). The near-antipodal pin does not, because glibc and libm agree there, and the near-identity pin *cannot*, because its branch uses only `sqrt`, which is correctly rounded in every implementation. All three still pin their distinct branch (trig / flip+trig / normalized-lerp) and run in both feature modes, which is what D10 asked for. (d) Constants are anchored to mathematics, not to the implementation's output: the interior pins sit one ulp from the exact `cos(0.305)`/`sin(0.305)`, and the near-antipodal pins are the correctly rounded `cos(0.005)`/`sin(0.005)` exactly (high-precision reference computed outside the crate). (e) Docs beyond the stage's named AGENTS.md clause, where this change falsified them: README (the "float math falls back to libm" line, plus a Performance bullet stating the both-modes guarantee), `lib.rs` Reliability bullet, `slerp` rustdoc, CHANGELOG behavior-change entry, and MIGRATION runtime item 8 — a 1.x user's slerped rotations can move by a few ulps, which "compile clean, behave differently" is exactly the section for. (f) Commit column reads "this commit" per the Stage 2 precedent (a row cannot carry the hash of the commit that contains it). Environment unchanged from the Stage 2 note: `rustup run nightly` supplies cargo/rustc 1.95, while clippy and rustfmt resolve to the Nix 1.99 nightly on `PATH`; the gate is green under that mix. **Review follow-up:** all three blockers verified real and independently reproduced; the first two are one defect (MIGRATION item 8), the third is a magnitude that had spread to four sites. (1)+(2) MIGRATION item 8 invented 1.x history and scoped the change to `std`. `v1.4.1:Cargo.toml` has no `libm` at all and `v1.4.1:src/geometry/quaternion/mod.rs` has no `cfg(feature)` — `.sqrt()`/`.acos()`/`.sin()` are unconditional — so 1.x ran the platform's math in *both* feature modes; the fork this stage deleted first appeared in 2.0.0-alpha.1. Reproduced by extracting the v1.4.1 tree and running this stage's interior fixture through it: `--no-default-features` and `std` both yield `3FEE85EA0B555D65 3FD33800DDAF16F5`, identical to each other and two/one ulps from the shipped pin. Item 8 now states the real baseline (1.x had no libm; both modes were the platform's), drops the `std` scoping in headline and advice, and quotes the measured bound — a 1.4.1 `default-features = false` user (the configuration 1.4.1's own README documents) was previously told their numbers could not move. (3) "up to one ulp per component" is false: an independent sweep on this box (identical operands through the platform path and the libm path, 1999 arcs over (0, pi) x 101 factors x 2 rotation axes, ~1.6M component comparisons) reproduced the reviewer's numbers exactly — worst **4 ulps** at 1.165531 rad, t = 0.39; distribution 49201 at 1 ulp, 12052 at 2, 493 at 3, 13 at 4. Corrected in CHANGELOG (four ulps, interior fixture moves two) and in the `bits()` doc comment in `src/geometry/quaternion/tests.rs`, which carried the same false "one ulp per component" and was not named by either reviewer. Deliberate non-edits: the CHANGELOG's historical clause ("called `f64`'s own … under `std` and `libm`'s without it") is *correct* in its rc.1 entry, which describes the delta from beta.4 — but it reads as 1.x history out of context, so the Final-stage 2.0.0 consolidation must keep it scoped to the 2.0 series; and `v2-fitness-audit.md:186`'s pre-implementation "~1 ulp" estimate stays as the dated record of what was known then (its conclusion, negligible against `UNIT_NORM_TOLERANCE`, survives at 4 ulps). Doc-only: the numbers themselves are already pinned by the three bit tests, so no new test. |
| 4 | Surface trims + renames (D5, D7, D8) | DONE | this commit | GREEN | Source drift is small but two sub-decision claims were wrong. (a) **Doctests**: the record names three (`buffer/mod.rs:157,183,210`) and assumes "doctests on non-public items don't run". There are **six** (module-level, the three constructors, `insert`, `get`), and rustdoc *does* collect them from private items — the moment `core` went private all six failed to compile (E0603), i.e. a hard gate failure, not silent rot. A doctest compiles as an external crate, so no form of them can reach a crate-private type: all six are deleted. Their coverage already existed in `src/core/buffer/tests.rs` (`insert_and_get`, `get_on_empty_buffer_reports_no_transforms`, the expiry tests); the one gap — `dynamic_with_max_age` observed on its own — is now a third arm of `get_on_empty_buffer_reports_no_transforms`, so all three constructors are unit-tested, which is what "convert to unit tests" was protecting. (b) **Dead code forced four deletions**: with `Buffer` `pub(crate)`, `child()`, `is_static()`, `len()` and `is_empty()` have no non-test caller, so the lib build warns `dead_code` and both `-D warnings` clippy steps fail; `#[allow]` is forbidden, so per prefer-deletion they are gone (`parent()` stays — `creates_cycle` and `frame_exists` call it). Every assertion they carried is restated as the behavior `Registry` actually observes: emptiness → `get` returns `NoTransformAvailable`; kind → the opposite insert returns `StaticDynamicConflict`; the child pin → a foreign child returns `ChildFrameMismatch`; the static upsert → the replacement is served at *every* instant. `kind_accessors_reflect_construction` is deleted as fully redundant with `insert_rejects_static_dynamic_mixing` + `get_on_empty_buffer_reports_no_transforms`; `frame_accessors_reflect_pinning` becomes `frame_pins_survive_the_buffer_being_emptied` and now asserts the child pin through the rejection it causes. No invariant lost coverage. (c) The private helper `delete_expired` is renamed `remove_expired` — one name beyond the two D8 lists, same purpose, same file. (d) **D7 needed no MIGRATION entry**: `v1.4.1` has no `Quaternion::new` (verified via `git show v1.4.1:src/geometry/quaternion/mod.rs`; the audit says the same at :121 — it arrived in 2.0.0-alpha.1), so a 1.x reader has nothing to migrate; 1.x built quaternions from struct literals, which still work and are already covered by MIGRATION's "What does not break". CHANGELOG carries it instead. README likewise needed no `Quaternion` edit: the record's "README:164/218" point at the Core-Types row and the `Transform` struct field, which name the type, not the constructor — README has no `Quaternion::new` call site. (e) The new MIGRATION privatization bullet quotes rustc's *actual* message, verified by compiling a scratch crate against this tree: `error[E0603]: module core is private` for **both** `transforms::core::Buffer` and `transforms::core::Registry` — but the help differs between them. `Registry` additionally gets `help: consider importing this struct instead`, proposing `use transforms::Registry;`, which is the right answer; `Buffer` gets no suggestion, because no public re-export exists for rustc to point at. (This is the corrected reading — the note originally recorded here claimed no suggestion for either import and was wrong; see the review follow-up below.) Stage 8's planned module-privatization bullet is therefore partly landed here, and the record's "rustc's E0603 suggestion is correct here" wording **holds as written for the `core` module** — Stage 8 must not scope it down. (f) Docs updated beyond the stage's named files where this change falsified them: `lib.rs` (architecture bullet — `Buffer` was advertised as "also usable standalone" — plus two `remove_transforms_before` mentions), `time/traits.rs` (custom clocks were said to be used with "`Transform`, `Buffer`, and `Registry`"), the buffer module doc (gains its internal status and where the tree invariants are enforced), README (`TimePoint` row, architecture intro, "### Buffer (internal)"), AGENTS.md (architecture line, cleanup invariant), MIGRATION (breaks 1, 2, 5, 6, the `Stamp`-section `Buffer` bullet, runtime change 5), and CHANGELOG rc.1: the `Buffer`-kind and `Buffer::get`-by-value entries are replaced/dropped because they describe API that now reaches no user in this unreleased cut (Stage 1's D4 precedent), `core::Buffer` leaves the canonical-path entry, two Fixed entries are restated at registry level, and the beta.5 doc-fix entry loses its "the crate docs no longer call the public `Buffer` type internal" clause — this stage makes "internal" true again. (g) rustfmt re-wrapped four call sites the longer `from_wxyz` name pushed over the width. Environment unchanged from Stages 2–3: `rustup run nightly` supplies cargo/rustc 1.95 while clippy and rustfmt resolve to the Nix 1.99 nightly on `PATH`; the gate is green under that mix. **Review follow-up:** five blockers filed, all verified real, collapsing to three defects — two documentation-accuracy defects plus this log's own note (e). (1)+(5) are one defect: MIGRATION break 5 said rustc "offers no replacement path" for the privatized `core` module. Independently re-reproduced on both toolchains in this environment (rustup nightly rustc 1.95.0 2026-02-27, and the Nix 1.99.0-nightly on `PATH`) with a scratch crate importing both paths: `transforms::core::Registry` *does* get `help: consider importing this struct instead` → `use transforms::Registry;`, and only `transforms::core::Buffer` gets a bare E0603. The bullet now says exactly that and tells the reader to take the `Registry` suggestion, explicitly contrasting it with break 1's "do not accept the compiler's suggestion" — break 1 had primed migrating users to distrust the one suggestion that is right. One correction to the reviewers' phrasing, recorded because MIGRATION deliberately does not claim it: the suggestion's `suggestion_applicability` is `MaybeIncorrect`, not `MachineApplicable` (read out of `--message-format=json`), so `cargo fix` will **not** apply it automatically — it is correct, but hand-applied. (2) note (e) above propagated the same false claim into resume-time ground truth and instructed Stage 8 to weaken the record's correct wording; corrected in place, and the instruction inverted — Stage 8 keeps `v2-decision-record.md:170-171` as written. (3)+(4) are one defect with two shipping sites, both re-verified against the code: `BufferError::StaticDynamicConflict`'s rustdoc (public via `errors::BufferError`, the return type of `Registry::add_transform`) still sent readers to `Buffer::static_edge`/`Buffer::dynamic`, items this stage made invisible — it now names what actually fixes the kind (the first insert's `Stamp`) and `Registry::remove_frame` as the only way to change it, matching the wording `Registry::add_transform`'s own `# Errors` already used; and the new buffer module doc inverted the enforcement split, claiming the frame-tree invariants are enforced in `Registry` "not here". Grep-confirmed false: `Buffer::insert` is the *sole* site constructing `SelfReferentialFrame`, `ReparentingNotSupported` and `ChildFrameMismatch`, while `Registry::process_add_transform` constructs only `CycleDetected` and only when `!data.contains_key(&t.child)` — i.e. the registry check *depends* on Buffer's pin. The module doc now states the real split and warns that the deferred Sample-split/ring-buffer rework must carry the pins forward, since dropping them leaves a re-parenting insert unchecked by anything. The same false claim shipped in CHANGELOG rc.1 (the `Buffer`-privatization entry), fixed there too. Not fixable here: `ff2bd6f`'s own commit message carries the identical claim in its D5 paragraph; rewriting history is out of scope for a follow-up commit, so it stands as a known inaccuracy in that message — the CHANGELOG and rustdoc are the surfaces a reader consults. Doc-only throughout, so no new test: nothing about behavior changed, and the enforcement split is already pinned by the existing `Buffer` and `Registry` insert-rejection tests. |
| 5 | Full enforcement (D2, D15) + double-inversion rework | DONE | 77c73a7 + follow-up | GREEN | No source drift: `Transform`, `Point`, `combine_transforms`/`reverse_and_invert_transforms`, `Buffer::insert`'s `validate()` and both backstops were all where the record said. Deviations and decisions: (a) **Sub-decision vs. stage bullet.** The `Point` sub-decision says its `Transformable` impl "now validates the *transform*"; the Stage 5 bullet says `Point::transform` *relies* on transforms being valid-by-construction and documents that as the trait precondition. The stage bullet won — no `validate()` call in `Point::transform`. With private fields the only reachable invalid transform comes from extreme composition, and a norm (hence `sqrt`) per point is a real cost where the trait is used on a whole scan. Recorded because a reviewer reading the sub-decision may expect the call. (b) The `pub(crate)` unchecked constructor is `Transform::unvalidated`, taking owned `String`s (every internal caller already owns them). A second crate-private helper was unavoidable: `Transform::restamped`, because the registry re-stamps a resolved chain at five sites and can no longer assign the field. (c) `Transform::validate` stays **public**. The stage does not ask for its deletion and it now has a stated job: it is what `Transformable`'s new precondition paragraph points third-party implementors at, and the only way to check a value produced by `*`. (d) **The record's "invalid transforms are unrepresentable" is not literally complete, and the docs do not claim it is.** `Mul` deliberately does not re-validate (audit 1.2: norm tolerances multiply across composition), so composing operands of extreme magnitude can still overflow a translation to infinity — and `Buffer::insert` no longer catches it. `inverse()` gained the translation-finiteness check the audit prescribed and rejects such a value; `Mul`'s rustdoc names the gap and points at `validate`. This is the one hole the private-fields rewrite leaves; flagged for the maintainer rather than closed, since closing it means either the validation the audit measured as harmful or a check the stage did not authorize. (e) **Both backstops deleted**, not kept. Termination is now argued in the rustdoc: the edge set is a functional graph that only cycle-checked inserts extend and `remove_frame` only shrinks. There is also an argument *against* the backstops — on a hypothetically corrupted tree, `get_transform_chain`'s counter converted the corruption into an ordinary "not found", i.e. a plausible-looking wrong answer, the failure mode this crate ranks worst. `creates_cycle` additionally loses a `BTreeSet` allocation per new-frame insert. (f) Tests whose subject the stage removed were deleted, not weakened: `insert_rejects_invalid_transforms` (buffer) and `add_transform_rejects_invalid_rotations` (registry) — the rejection moved to the constructor and is pinned by `new_rejects_transforms_that_would_corrupt_every_lookup` plus two new wire tests. `failed_insert_does_not_bypass_cycle_detection` keeps its invariant but had to switch its rejection cause to `SelfReferentialFrame`: a first insert can no longer fail on values. `identity_is_static` became `static_between_stamps_a_custom_time_transform_as_static`; the two `validate_*` proptests now target `Transform::new`. (g) **Every number in the docs is measured here, not quoted from the audit.** A counting global allocator on this box: single-hop lookup in the documented direction 11 -> 5 allocations (1456 B -> 488 B), four hops 23 -> 11, 64 hops 271 -> 135 — matching the audit's figures except its post-fix 1-hop prediction of 6 (actual 5). The bit-exactness claim was verified against the pre-stage tree in a throwaway worktree: the old path returned a translation 10 ulps off in x and 1 in z for the new regression test's fixture, so `single_hop_lookup_returns_the_stored_transform_bit_for_bit` genuinely fails on the old code. (h) Docs updated beyond the stage's named files where this change falsified them: `lib.rs` (the Reliability "validated inputs" bullet and two doc examples), `time/stamp.rs` (its `Transform::timestamp` reference is a method now), the buffer module doc and `Buffer::insert`'s rustdoc, `Registry::add_transform`'s `# Errors`, README (highlights, serde note, Quick Start, core-types table, the whole `Transform` API block, static/dynamic + Point + no_std snippets, two Performance bullets), MIGRATION (break 2, the `Stamp` example, a new break 7, runtime change 9, "What does not break"), CHANGELOG (five entries plus the `Stamp` entry's literal wording), AGENTS.md (three invariant clauses rewritten, one added for the chain-composition direction, the construction style rule, and a `#[must_use]` clause the crate's first fallible constructors made necessary). (i) **For the Final stage:** the historical beta CHANGELOG entry "`add_transform` returns `Result` and validates on insertion" is true of the release it describes but contradicts this stage's entry once the pre-release sections are consolidated into one `[2.0.0]`. (j) Environment unchanged from Stages 2-4: `rustup run nightly` supplies cargo/rustc 1.95 while clippy and rustfmt resolve to the Nix 1.99 nightly on `PATH`; the gate is green under that mix. **Review follow-up:** two blockers filed, both verified real, and they are one defect — deviation (d) above understated its own finding. Reproduced with an out-of-crate path-dep probe (the one the interrupted adversarial reviewer had been building): at `77c73a7`, two operands of norm 1.000001 — inside `UNIT_NORM_TOLERANCE`, i.e. the f32-widened rotation the tolerance exists to accept — compose to norm 1.000002000001, `validate()` rejects it, and `add_transform` returns **`Ok(())`** and serves it; two finite `1.0e308` translations compose to `inf` and are likewise stored and served; the ordinary flatten-and-republish pattern (`reg2.add_transform(reg1.get_transform("f0","f12",t)?)`) stored norm 1.0000120000659993, 12x the tolerance. All three returned `Err(BufferError::TransformError(..))` at `77c73a7^`. **The sub-decision authorizing the deletion is falsified, and says so itself:** "`Buffer::insert`'s `validate()` call becomes dead once `Transform` is valid-by-construction" is false because `Mul`, `interpolate`, `inverse` and every lookup are public and *documented as not re-validating*, and its acceptance criterion — "a test proving invalid transforms are unrepresentable" — is unsatisfiable, which is the signal. Note (d) recorded the hole and flagged it for the maintainer instead of closing it; that was the wrong call for a silent-wrong-answer path, and the review was right to escalate. **Fix:** `transform.validate()?` restored as the first statement of `Buffer::insert`, exactly where it sat at `77c73a7^`. That is the sole ingest funnel — `Registry::process_add_transform` is its only caller, at both of its two sites — so one line closes it, O(1) per insert. It is *not* the re-validation the audit measured as harmful: `*`, `interpolate`, `inverse` and lookups still do not re-check, so long-chain drift is still never rejected mid-lookup; only a value entering storage is checked, as in 1.4.1. No public surface change — `BufferError::TransformError` already exists and `add_transform` already returns `BufferError`. Taken under AGENTS.md's "prefer a loud error over a silent guess" rather than maintainer sign-off, which was not obtainable in an autonomous run; **the maintainer can still choose the hole, in which case only the code line reverts — every doc site below stays corrected.** Two regression tests, both verified failing with the line commented out: `insert_rejects_invalid_transforms` returns to `src/core/buffer/tests.rs` rebuilt on `Transform::unvalidated` (the AGENTS.md style rule for a deliberately invalid transform), now also asserting that a rejected insert pins no frames and that an f32-grade norm is still accepted; `add_transform_rejects_a_republished_chain_that_left_validity` is new in `src/core/registry/tests.rs` and uses **only the public API** — it composes the two in-tolerance operands into an out-of-tolerance product and the two finite translations into an infinite one, so it pins the user-reachable path, not a crate-internal shortcut. All five doc sites the review named are corrected: the buffer module doc and `Buffer::insert`'s rustdoc (which now say why the check is *not* redundant with the constructors), `Transform`'s type doc (the "a `Transform` that exists is valid" / "the only ways to build one" pair is gone — it contradicted the paragraph three lines below it), the `lib.rs` Reliability bullet, CHANGELOG (validation is *added* at construction, not moved there — `add_transform` still reports both variants) and MIGRATION break 2 (a 1.4.1 user's existing `add_transform` error handling still covers them; this was the site where the false claim would have cost the most). Three further sites carried the same claim and were not named by either reviewer: `Transformable`'s `# Precondition` paragraph in `src/geometry/transform/traits.rs` asserted "every transform derived from valid ones stays within tolerance" — false by the same example, and it is the paragraph third-party implementors are told to rely on; `BufferError::TransformError`'s variant doc said "failed during retrieval" (insertion again, too); and MIGRATION break 7's "the only ways to build one" is now "the only ways to build one from components". AGENTS.md gains an invariant clause pinning the ingest check with the reason, so the next prefer-deletion pass does not re-delete it. README needed no edit: its Performance section makes no insert-cost claim and "validated at construction" stays true. Item (d) above stands as written only for the part that is still true — `Mul` can still *produce* an invalid transform, and `Mul`'s rustdoc still names that and points at `validate`; what is no longer true is "`Buffer::insert` no longer catches it". |
| 6 | Error overhaul (D6) | DONE | 71a275e + follow-up | GREEN | Source drift is minimal: the only shift is the record's `tests/tests.rs:119-143` triple-nested match, which sits at 126-152 after Stage 5's constructor rewrite; located by intent. Deviations and decisions: **(a) `BufferError` is not merely un-exported, it is replaced by two crate-private types split by operation** — `InsertError` and `GetError<T>` in `src/core/buffer/error.rs`. One enum for both operations forces an unreachable arm on *every* conversion into `RegistryError` (an insert cannot fail for want of data; a read cannot fail on a frame pin), which is the shape D6 exists to end; split, both conversions are total. `GetError::OutOfRange` also carries the covered range in `T` rather than seconds, which is what `NotFoundAt { covered: Option<(T, T)> }` needs — the `as_seconds_lossy` conversion moves out of `Buffer::get` and into `Display`, its only remaining caller. **(b) `ChildFrameMismatch` — the one judgement call, flagged for the maintainer.** The sub-decision says it is "internal only", and it is genuinely unreachable through `Registry` (buffers are keyed by child and only a successfully-pinned buffer enters the map), but Stage 4's review recorded `Buffer::insert` as the *sole* enforcement site for the child pin, so the check stays and its conversion needs a target. Chosen: `RegistryError::TransformError(TransformError::IncompatibleFrames { expected: pinned, found })`, and the internal variant now carries both frame names instead of only the pinned one. `IncompatibleFrames` was already the crate's "the frames are not the pair this operation requires" error — `Transform::interpolate` returns it for an endpoint frame-pair mismatch, which is not a composition — so its doc's "parent-child composition" claim was already too narrow and is corrected to name the three real uses. Alternatives rejected: a public `RegistryError::ChildFrameMismatch` contradicts the sub-decision and would document a variant that cannot occur; deleting the check to make the state unrepresentable is a safety deletion this stage did not authorize (and Stage 4 warned the deferred storage rework must keep the pins); mapping onto `ReparentingNotSupported` or `SelfReferentialFrame` would be a semantic lie in the crate that ranks plausible-looking wrong answers worst. **(c) A wrapping arm was unavoidable.** The sketch's variant list covers diagnosis but not the geometry/time failures a *resolved chain* can produce — `inverse()` → `QuaternionError`, interpolation → `TimeError::DurationOverflow`, composition frame/time mismatches. Flattening nine more variants would duplicate `TransformError` wholesale, so `RegistryError::TransformError(TransformError)` exists as the single wrapping arm; every diagnosis path and every insert rejection stays one level. **(d) Canonicalization, because of (c).** With that arm present, `NonUnitRotation`/`NonFiniteValues` would have had two spellings — flat from an insert, wrapped from a lookup — and a caller matching one would silently miss the other. A hand-written `From<TransformError> for RegistryError<T>` rewrites those two into the flat variants at every conversion site; `#[from]` is deliberately *not* used on the wrapper arm, because thiserror's generated `From` would bypass the rewrite. This is reachable, not theoretical: two finite `1.0e308` hops compose to an infinite translation and the lookup's inversion reports it, pinned by the new public-API test `an_overflowing_lookup_reports_the_flat_variant_only_where_it_inverts` (renamed in the review follow-up below; the name recorded here originally — `a_lookup_that_overflows_reports_the_same_flat_variant_as_an_insert` — generalized the inverting case, which is precisely the defect the review found in the shipping docs). **(e) `Disconnected`/`NotFoundAt` keep the `_frame` suffixes** rather than the sketch's `{ target, source }` shorthand: a `String` field named `source` is claimed by thiserror's source-chaining convention and does not compile. This is the crate's existing, already-documented resolution of the same clash. **(f) `NotFoundAt`'s `Display` renders through a private zero-allocation adapter** (`Coverage`) instead of an `alloc::format!` inside the `#[error(...)]` attribute: an error message that can fail to be built — on a failing allocation — is worse than the error it reports. Pinned by `not_found_at_renders_both_coverage_cases_in_seconds`, which also pins that both `covered` shapes format through `as_seconds_lossy` and that they read differently (a drained frame is not a timing problem). **(g) One lookup failure loses its frame name.** A mid-walk *interpolation* failure (`GetError::Interpolation`) is now reported as the underlying failure instead of as `NotFoundAt`, because the frame does cover the requested time and neither `covered` shape would describe it truthfully; previously it arrived as `NotFoundAt { frame, source: BufferError::TransformError(..) }`. Documented in `get_transform`'s `# Errors` and at `diagnose_not_found`. Unreachable with the default `Timestamp` (u64 nanoseconds: every difference is a valid `Duration`); reachable only for a custom clock whose `duration_since` can overflow. **(h)** `TransformError` loses `UnknownFrame`, `Disconnected` and `NotFoundAt` per the bullet, and with them the `Box<BufferError>` — the mutual recursion the audit filed (finding 1.x, `BufferError::TransformError` ↔ `TransformError::NotFoundAt`) is gone, not merely hidden. **(i) Docs updated beyond the stage's named files where this change falsified them:** README (a new What's-New bullet, the four `Registry` signatures, and a paragraph naming the taxonomy), MIGRATION (break 2, break 3 rewritten around `RegistryError` with a compiling-shaped one-level match, the `Stamp`-migration claim that a mis-migrated static publisher fails with `TimestampOutOfRange`, the same claim in the JSON cross-version bullet, and runtime changes 5 and 6 — all four said `TimestampOutOfRange`/`NoTransformAvailable` where a *lookup* now reports `NotFoundAt` with `covered`), CHANGELOG (a new rc.1 Changed entry plus a correction to the "every error payload field is named" entry, whose "which `NotFoundAt`'s boxed `BufferError` keeps" clause is now false), AGENTS.md (the static-xor-dynamic invariant's error name, the lookup invariant's payload description, and one new invariant clause pinning flatness, the canonicalization rule and the internal split so the next prefer-deletion pass does not merge the two buffer enums back together), the `errors` module doc, the `Buffer` module/type/`insert`/`get` rustdoc, `Registry`'s four public `# Errors` sections and `remove_transforms_before`, and `TransformError::IncompatibleFrames`'s variant doc. **(j)** `public_types_are_send_and_sync` gains `RegistryError`: `lib.rs` claims all types are `Send + Sync` and this stage adds a public type. **(k) For the Final stage:** the `[2.0.0-beta.3]` CHANGELOG section describes the lookup diagnosis as `TransformError::UnknownFrame`/`NotFoundAt` "carrying the `BufferError` as the error source". True of that release, contradicted by this stage once the pre-release sections are consolidated into one `[2.0.0]`. **(l)** Environment unchanged from Stages 2-5: `rustup run nightly` supplies cargo/rustc 1.95 while clippy and rustfmt resolve to the Nix 1.99 nightly on `PATH`; the gate is green under that mix. **Review follow-up:** three blockers filed, all three verified real and reproduced — two documentation-accuracy defects plus one over-claim in shipping rustdoc. **(1) AGENTS.md's interpolation invariant** still said a query outside the covered range "fails with `TimestampOutOfRange`". Falsified by this stage for every registry path: `Buffer::get` returns `GetError::OutOfRange` and `diagnose_not_found` turns it into `RegistryError::NotFoundAt { covered: Some((start, end)) }`; a tree-wide grep leaves `Transform::interpolate` (`src/geometry/transform/mod.rs:388`) as the sole constructor of `TimestampOutOfRange`, and `Buffer::get` calls `interpolate` only when both neighbours exist — never out of range. The bullet two entries above it, rewritten by this very commit to describe `covered: Option<(T, T)>`, said the opposite, so the file that is normative for future agents carried two contradictory contracts; the identical claim's twin in MIGRATION runtime change 6 *was* corrected in 71a275e, making this a missed site of a correction the commit already knew it had to make. Now states that no registry path produces `TimestampOutOfRange` and names its only producer. **(2) CHANGELOG rc.1 `Fixed`** still described `NotFoundAt`'s two causes as `TimestampOutOfRange` vs `NoTransformAvailable`. Neither name is observable by an rc.1 user — `NoTransformAvailable` is a variant of the crate-private `GetError`, `TimestampOutOfRange` is unreachable from any lookup — so one release's notes carried two contradictory descriptions of the same variant, and this is the entry that tells a migrating user a drained frame is not a retry problem. Restated as `covered: Some(range)` / `covered: None`, matching the same section's `Changed` entry; same precedent as Stage 1 (D4) and Stage 4 note (f) — an rc.1 entry describing API that reaches no user in this unreleased cut gets replaced, not annotated. **(3) The one scope error, and the consequential one.** Reproduced with an out-of-crate path-dep probe against 71a275e: with `a -> b -> c` built from two individually finite, individually accepted `1.0e308` translations, `get_transform("a", "c", t)` returns **`Ok`** with `translation.x == inf`, and only `get_transform("c", "a", t)` returns `Err(NonFiniteValues)`. Cause confirmed in `combine_transforms`: the ancestor-ward lookup lands in the `(None, Some(source))` arm and never calls `inverse()`, which is the only finiteness check on any lookup path — `Mul`/`compose_ignoring_time` do not check. The probe additionally showed `get_transform_at` inheriting it in both fixed-frame shapes tried, which the reviewer had not tested. Four doc sites added by this commit generalized the inverting case into a universal (`get_transform`'s `# Errors`, `RegistryError`'s type doc, MIGRATION break 3, the CHANGELOG rc.1 flat-error entry); a fifth neither reviewer named carried it too — `RegistryError::NonFiniteValues`'s own variant doc, true as far as it went but reading as complete. The commit *message* was accurate; only the shipping docs over-claimed, and they contradicted `Transform`'s type doc (`src/geometry/transform/mod.rs:40-50`) and the AGENTS.md derived-values invariant, both of which already said a lookup result is not re-validated. All five are now scoped to the inverting half-chain and say plainly that a lookup toward an ancestor returns the overflowed translation as `Ok`, with `Transform::validate` named as the caller's remedy. This is the reviewer's option (a) taken deliberately: option (b), a finiteness check on the non-inverting path, is a behavior change contradicting a written AGENTS.md invariant and exceeds Stage 6's scope. Test: `a_lookup_that_overflows_reports_the_same_flat_variant_as_an_insert` is renamed `an_overflowing_lookup_reports_the_flat_variant_only_where_it_inverts` — the old name asserted the same universal the docs did — and gains the ancestor-ward case, asserting `Ok` with an infinite x translation, so the scope is pinned and any future behavior change trips it. Sweep for the same claims elsewhere found no further stale site: README's taxonomy paragraph lists insert-side variants without claiming lookup reachability, and MIGRATION break 2 and CHANGELOG:171 already state the no-re-validation rule correctly. Doc-only plus the one test; no behavior changed. **ESCALATION — unresolved, carried forward from Stage 5 deviation (d): `get_transform` reports an overflowed chain in one direction and returns it silently in the other, and the silent one is the documented primary direction (`get_transform("map", "lidar", t)`).** On finite, individually accepted inputs the public API can hand back an infinite pose with `Ok` — the plausible-looking-wrong-answer mode AGENTS.md ranks worst. Closing it costs one `is_finite` triple on the composed translation: no norm, no `sqrt`, hence none of the composition drift the audit measured as harmful, and `inverse()` already sets that precedent by checking exactly this and nothing else. Not taken autonomously because AGENTS.md's "values derived from valid transforms — `Mul`, `inverse`, `interpolate`, every registry lookup — are deliberately not re-validated" is normative, and a lookup that begins returning `Err` where it returned `Ok` is a behavior change no stage in this program authorizes. **Needs the maintainer's yes or no before 2.0.0**; if the answer is to close it, the new test's second assertion inverts and the five doc sites revert to the universal claim they made at 71a275e. |
| 7 | Wire format (D13, D14) | DONE | this commit | GREEN | No source drift: `Stamp`'s hand-written `Serialize`/`Deserialize`, the `deserialize_with` detour on `TransformRepr.timestamp` and `Timestamp`'s plain derive were all where the record said. **The record's central prediction holds: postcard golden bytes are byte-identical, no stop-and-look needed.** Both golden tests pass with their arrays untouched — an externally tagged enum puts variant index 0/1 exactly where the `Option` tag sat, and a one-field struct was already serialized as just its field, so `transparent` moves nothing. Only the comments naming those bytes changed (`Some`/`None` → variant 1/0). Deviations and decisions: **(a) The three negative wire tests land as one new function plus two extended ones**, not three new functions. `null` gets its own (`transform_with_a_null_timestamp_is_an_error`) because it is the behavior D13 changes; the NaN-*rotation* and norm-2 cases are second cases inside the existing `deserializing_a_non_finite_component_is_an_error` (now patching `rotation.w` at byte 24 as well as `translation.x` at byte 0) and `deserializing_a_denormalized_rotation_is_an_error` (now 1.01 and 2.0), because each reaches a `validate()` branch its existing case already reaches — a separate function per literal would be duplication, not coverage. **(b) One test beyond the bullets: `timestamp_is_a_bare_integer_on_the_wire`.** D14's entire content is a JSON shape, and nothing else in the suite constrains it — every roundtrip test passes with or without `transparent`, and the postcard goldens are identical either way, so a dropped attribute would have been invisible. Verified failing without it (`{"t":1000000000}`). **(c)** `static_transform_serializes_timestamp_as_null` is renamed `static_transform_serializes_its_stamp_as_the_static_tag`: its name asserted the encoding this stage deletes. **(d) Both new assertions verified failing against the pre-stage code**, by stashing only the three source files: the null case returned `Ok(Transform { .. timestamp: Static .. })` — the exact silent mint of an eternal transform D13 exists to close — and the bare-integer case returned `{"t":1000000000}`. **(e) MIGRATION's wire section is rewritten, not patched.** It opened by comparing to a 1.x wire format that does not exist: `v1.4.1:Cargo.toml` has no serde at all (the feature arrives in 2.0.0-alpha.1, per the alpha section of the CHANGELOG), so "a dynamic transform's shape is unchanged" claimed continuity with nothing. The section now says that outright and frames itself as the format you encode *into*, keeping both cross-version bullets scoped to hand-rolled 1.x-shaped payloads. The JSON bullet inverts: such a payload used to decode as a dynamic sample at the epoch and now hard-errors (`unknown variant 't', expected 'Static' or 'At'` for the `{"t": ...}` record; a bare timestamp is rejected as not an enum — both reproduced). The postcard bullet survives in substance, including the `t = 0` exception, which still reads as variant 0 and still decodes as `Stamp::Static`. **(f) Docs updated beyond the stage's named files where this change falsified them:** `Stamp`'s type doc (its serde paragraph described the optional encoding), `Timestamp`'s type doc (gains the transparent shape and why it matters to a foreign-language consumer), and three CHANGELOG entries other than the rewritten serde one — the `Timestamp`-narrowing entry, whose "the serde wire shape is unchanged … a JSON integer" conflated width with shape and is now scoped to the width (which is all that entry changed); the Stage 5 deserialize-validation entry, whose bare "the wire format is unchanged" reads as a claim about *this* stage's format once the pre-release sections consolidate, now "without touching the wire format"; and the beta golden-bytes entry, since `Stamp`'s variant order is part of the wire contract now too. Per the Stage 1/4/6 precedent the optional-encoding entry is **replaced**, not annotated — it describes an encoding no released version ever shipped. **(g) AGENTS.md** gains the tagging rule inside the existing static-xor-dynamic invariant, with its reason: trading the derive back for an `Option`-shaped impl is not a formatting preference, it makes a dropped or nulled stamp decode as `Static`, and that is the silent-wrong-answer mode the file ranks worst. `Transform`'s type doc needed no edit — the missing/null contract now lives on `Stamp`, where the encoding is, rather than on the `TransformRepr` field comment the detour's deletion removed. **(h)** Net diff on the source is negative: 34 lines of hand-written serde in `stamp.rs` and the 8-line detour in `transform/mod.rs` are gone, replaced by two `cfg_attr` lines. **(i)** Environment unchanged from Stages 2-6: `rustup run nightly` supplies cargo/rustc 1.95 while clippy and rustfmt resolve to the Nix 1.99 nightly on `PATH`; the gate is green under that mix, with zero warnings in the whole run. **Review follow-up:** three blockers filed, all verified real and independently re-reproduced; they are two defects across four shipping sites. Deviation (b) above — "the record's central prediction holds" — was **true only for postcard, and the commit generalized it to every non-self-describing format.** Reproduced with my own out-of-crate path-dep probe (bincode 1.3.3, bincode 2.0.1, postcard 1.1.3) built against three trees extracted with `git archive`: `578e4c9^`, `578e4c9`, and **`v2.0.0-beta.4`** — the last *released* cut, which the reviewers had not measured and which turns out to decide the fix. Fixture `map`→`base_link`, translation (1.5, -2.25, 0.0), identity rotation, `t = 1_753_142_400_000_000_000`. **Measured, stamp region only:** postcard and bincode 2 `config::standard()` are byte-identical across `578e4c9` (80 dynamic / 71 static, both trees); bincode 1.x `bincode::serialize` and bincode 2 `config::legacy()` are **not** — `[01]` + 8-byte LE u64 becomes `[01 00 00 00]` + 8-byte LE u64 (93 → 96 bytes) and the static arm `[00]` becomes `[00 00 00 00]` (85 → 88), because bincode's fixint config writes an enum variant index as a fixed 4-byte `u32` while writing an `Option` tag as one byte, and writes an integer as a fixed 8-byte LE value, never LEB128. Cross-decoding a pre-stage bincode 1.x record with post-stage code errors — `invalid value: integer 2902458369, expected variant index 0 <= i < 2` (dynamic), `integer 768` (static), and `io error` for `t = 16777216`, whose zero low bytes let the variant misparse as `At` before the shifted window dies — so the failure is loud per record, but the doc told integrators there was nothing to re-encode. **The fix goes past the reviewers' suggested one, because the baseline they scoped to does not exist.** Both reviewers proposed narrowing "byte-identical … so postcard and bincode streams are unaffected" to postcard + bincode 2 standard. But the encoding it compares against — the `Option`-shaped one — **never shipped**: `Stamp` appears nowhere in the released alpha/beta CHANGELOG sections, and `git show v2.0.0-beta.4:src/geometry/transform/mod.rs` has a bare `pub timestamp: T`. Measured against beta.4, the same fixture goes 79 → 80 bytes in postcard (a tag byte where there was no tag at all) and 100 → 96 in bincode 1.x (+4 index, −8 timestamp width), so a narrowed sentence would still have promised beta.4 users — the only users with streams — that their postcard data was unaffected, which is false. The sentence is therefore **replaced** rather than scoped, per the Stage 1/4/6 precedent for rc.1 text describing something no released version shipped: it now says every format changes shape, tells the reader to re-encode or version-tag, and gives the codec-dependent index width (1 byte postcard / bincode 2 standard, fixed 4-byte `u32` bincode 1.x / bincode 2 legacy) with both measured deltas. `CHANGELOG:46`'s "a 1-byte variant index elsewhere" is corrected to "a variant index ahead of the payload elsewhere" — width is the codec's, not this crate's. MIGRATION's wire paragraph gets the same treatment, and its "the bare LEB128 varint it always was" is doubly wrong (bincode fixint never emits LEB128, and 1.x had no serde for anything to "always" have been); it now names both configs' shapes and tells a hand-rolling integrator to emit what their config specifies. **Blocker 3** (the `Timestamp`-narrowing entry's "every representable value encodes to the same bytes it did as a `u128`") is confirmed by direct measurement — bincode 1.x and bincode 2 legacy write 8 bytes for `u64` against 16 for `u128`; postcard, bincode 2 standard and JSON are identical — and by beta.4's own 16-byte stamp blob. The pre-stage enumeration is restored and extended (JSON integer / postcard varint / bincode 2 standard varint, golden vectors included), with the fixed-width shrink stated instead of contradicted three words later by the MessagePack clause. **One site neither reviewer named, carrying the same false universal:** MIGRATION's `Timestamp` bullet in section 6 (~:247) said "The narrower width costs nothing on the wire … so only a stamp beyond 2554 stops decoding" — its em-dash clause was correctly scoped to JSON and postcard, but headline and conclusion generalized, and this is the bullet a beta user re-encoding archived stamps reads. Scoped the same way. **Deliberate non-edits, each verified rather than assumed:** `README:91` advertises bincode 1.x/2.x and MessagePack support and needed no change — a round-trip probe over `Stamp::Static`, `t = 0`, `t = u64::MAX` and the wall-clock fixture through `bincode1`, bincode 2 standard *and* legacy, postcard and `serde_json` returns an equal `Transform` in all 20 combinations, so "round-trip the full range" is true; the claim that broke was byte *identity*, not support. `Stamp`'s rustdoc (`src/time/stamp.rs:38`) says "a variant index followed by the payload" with no width — correct as written. The "1-byte variant index"/"LEB128 varint" comments in `tests/serde.rs:288,340` sit inside the postcard-only golden tests and are accurate there. **No new test, and this is a gap worth the maintainer's attention:** pinning bincode bytes needs `bincode` as a dev-dependency, which AGENTS.md forbids without maintainer approval, so the golden-bytes tests still freeze postcard alone — a future change to `Stamp`'s variant shape stays machine-checked for postcard and doc-checked everywhere else. Finally, `578e4c9`'s own commit message contains the same over-claim ("Non-self-describing formats are unaffected, which the record predicted"); rewriting history is out of scope for a follow-up, so it stands as a known inaccuracy in that message, per the Stage 4 precedent — CHANGELOG and MIGRATION are the surfaces a reader consults. |
| 8 | Docs true-up (D9 + Part-2 doc items) | DONE | this commit | GREEN | Source drift is line numbers only: the four stale-rustdoc lines the record calls `src/lib.rs:165,167` sit at 171/173 after Stages 2–5, and `registry/mod.rs:22-26` is unmoved; both located by intent. Deviations and decisions: **(a) One stage bullet was already discharged and is a verified no-op.** "Fix the bullet attributing 2.0-beta constructor names to 1.x" targets the base tree's `Stamp`-section bullet, which claimed 1.x had `Buffer::new()` / `Buffer::with_max_age(d)` / `Default`; `git show v1.4.1:src/core/buffer/mod.rs` has only `new()` (no_std) and `new(max_age)` (std) and no `Default` impl, so two of those three names were 2.0-alpha inventions. Stage 4 deleted that bullet wholesale when `Buffer` went private. Every remaining 1.x-attributed name in MIGRATION.md was then checked against the v1.4.1 tree rather than assumed — `Registry::new(max_age)`, `Buffer::new(max_age)`, `Vector3::new`, `Quaternion::identity`, `Timestamp::zero/now/as_seconds_unchecked`, `Timestamp { t: u128 }`, `Transform { .. }` literals, `Transform::identity`, `TransformError::NotFound`, the deprecated `TimestampError` alias, `BufferError::MaxAgeInvalid`, and `TimePoint::{static_timestamp, is_static, checked_add, as_seconds}` — all are real 1.4.1 API, so nothing else needed correcting. **(b) Every number in the docs is measured on this box, not quoted from the audit** (Stage 5 precedent), with a counting global allocator in an out-of-crate path-dep harness. Resident memory is **~320 B per stored sample**, not the audit's 327: 262 B of requested blocks with realistic frame names (243 B with two-character ones), plus 32-byte allocator block granularity → 321 B, stable from 1k to 100k samples. `Transform` is 120 B now, not the audited 144 — the u64 stamp narrowing (Stage 2) took 24 B off, which the ordered map's node fill partly absorbs. The record's "expect similar" holds. Allocation counts re-measured and identical to the figures Stage 5 already put in the README (1 hop 5 ancestor-ward / 6 reverse, 488 B churn; 4 hops 11; 64 hops 135), so that bullet needed no restatement — only verification. Timings on an i7-1065G7, release+LTO: insert in steady state 0.41–0.43 µs, 1-hop 0.60 µs exact / 0.72 µs interpolated, 4-hop ancestor-ward 1.51 µs exact / 1.85 µs interpolated, unknown-frame diagnosis over 1000 frames 8.3–8.8 µs, and a 1 kHz tick of 6 published edges plus 3 lookups of 3–5 hops at 10.5–11.1 µs (≈1% of the tick). **(c) The MCU rows of the envelope table are labeled `estimated`, and the M7 row `neither measured nor estimated`.** The record says "from the audit's measured numbers", but the audit's MCU column was itself a first-principles estimate it caveats at ±2×, and its x86 column predates Stages 2 and 5; publishing either as measured would be the failure mode this crate ranks worst, in a table whose whole purpose is to stop users self-selecting into the failure region. The memory column *is* arithmetic on the measured coefficient and is stated as an upper bound on 32-bit targets, where the pointer-sized fields halve. **(d) The README concurrency snippet is example-backed, not doc-tested** — the record allows either. Doc-testing README snippets means `#![doc = include_str!("../README.md")]` at the crate root, which would pull every fragment in the file into rustdoc and is a crate-wide change this stage does not authorize; instead the snippet now matches `examples/std_full.rs`, which the gate compiles and runs, and points at it. **(e) The E0603 claim was re-verified for all seven privatized paths**, not assumed from Stage 4's `core`-module finding: `geometry::{transform, quaternion, vector3, point}`, `time::{timestamp, traits}` and `core::registry` each get a correct `help: consider importing this struct/trait instead` naming the canonical re-export, all with `suggestion_applicability: MaybeIncorrect` (read out of `--message-format=json`), so `cargo fix` will not apply them. The record's `:170-171` wording therefore stands as Stage 4's review instructed, and `core::Buffer` — the one path with no public re-export and so no suggestion — stays as the explicit contrast. **(f) `Quaternion::normalize`'s overflow claim was wrong too and is fixed in the same sentence.** The stage names only the `1e-8` zero threshold (actual: a norm below `f64::EPSILON`, wrong by eight orders of magnitude in the permissive direction, copied from the `Div` impl — whose own stricter-threshold doc is correct and untouched). The same sentence also claimed components beyond `1e150` overflow the norm; measured, `1e150` in all four components normalizes fine and the real boundary is `sqrt(f64::MAX)` ≈ 1.34e154. Leaving half a corrected sentence false was not an option. The boundary doctest pins both sides (`f64::EPSILON` normalizes to the identity, `f64::EPSILON / 2.0` is `ZeroLengthNormalization`) and was **verified failing** against the previously documented threshold by temporarily setting the guard to `1e-8`. **(g) `examples/std_full.rs` lags two publishing periods, not one.** One period puts the query stamp exactly on the newest sample and races the writer's next insert — a flaky example on a loaded box. The reader also sleeps `PUBLISH_PERIOD + LOOKUP_LAG` before its first query, so the run logs no `NotFoundAt` at all; the header states why a real reader must query behind `now()`. **(h) Docs updated beyond the stage's named files:** AGENTS.md's Non-Goals clause gains the f64 commitment and the "README and crate root carry the same list verbatim — edit both or neither" rule that the identical-lists requirement implies for future agents. **(i) CHANGELOG stage coverage audited decision by decision**, per "entries for every stage": D1–D3, D5–D8, D10–D15, the invert-once rework and this stage all have rc.1 entries; D4 deliberately has none (Stage 1: `Stamp` ships first in this unreleased cut, so the ordering derives never reached a user — the rationale lives in `Stamp`'s rustdoc and AGENTS.md). The existing `Registry::new()`-shorthand entry was **extended rather than duplicated**, since this stage fixes the rustdoc half of the same defect. **(j)** Environment unchanged from Stages 2–7: `rustup run nightly` supplies cargo/rustc 1.95 while clippy and rustfmt resolve to the Nix 1.99 nightly on `PATH`; the gate is green under that mix. |
| 9 | Test & bench hardening | PENDING | — | — | — |
| Final | Full gate + semver-checks vs v1.4.1 + bump 2.0.0-rc.2 | PENDING | — | — | — |

Status values: `PENDING` → `IN PROGRESS` → `DONE` (or `BLOCKED` with a reason in Notes).
Review findings that led to a follow-up commit are noted in the same row.

## Interruption record — 2026-08-15, after Stage 2

Deliberate clean termination by the maintainer. Working tree clean at `f49c6f0`; Stages 1–2 are
implemented, gated, and committed. Stage 2's **post-commit review was cut short**:

- The adversarial reviewer filed one blocker, **verified real before shutdown**: the crate-root
  panic-policy rustdoc (`src/lib.rs:192-195`) still claims the *single* documented panic cause is a
  pre-epoch clock, but the u64 narrowing added a second reachable cause (`Timestamp::now()` on a
  clock past `u64::MAX` ns, mid-2554). The commit updated `Timestamp::now`'s rustdoc and AGENTS.md
  but missed this crate-root site. **Resolved** on resume by the "Address Stage 2 review findings"
  follow-up commit; see the Stage 2 row.
- The fidelity reviewer was terminated mid-run; its verdict is unknown. Optionally re-review
  `f49c6f0` (spec-fidelity against the record's Stage 2 + sub-decisions) before moving on; its
  last observed activity (leftover-`u128`/`Duration` sweeps, cross-target builds) had surfaced
  nothing at termination time.

Then proceed with Stage 3 per the protocol above. (Both items were resolved on resume — see the
Stage 2 row and commits f1a401b/b3a98f9; f1a401b also absorbed a toolchain drift, the newer nightly
deprecating the `std::f64` module constants the quaternion tests used.)

## Interruption record — 2026-08-15 evening, during Stage 3 review

Second deliberate clean termination by the maintainer. Working tree clean at `b2026be`; Stages 1–3
are implemented, gated, and committed (Stage 3 = libm everywhere, D10). Stage 3's **post-commit
review had filed its verdicts but the fix agent was stopped before editing anything.** Both
reviewers verified their findings with reproduced evidence (extracting and building v1.4.1,
ulp sweeps on both math paths). Two real defects, both documentation-accuracy in `b2026be`:

1. **MIGRATION.md runtime item 8 (~:213-220) invents false 1.x history** — it says 1.x used
   `f64`'s methods under `std` and "libm's without it". Verified false: `v1.4.1:Cargo.toml` has no
   libm dependency and its quaternion code calls `.sqrt()`/`.acos()`/`.sin()` unconditionally —
   1.x used platform math in both modes; the libm fallback only appeared in 2.0.0-alpha.1. The
   item must describe the real baseline (and not scope the change to `std` builds only).
2. **CHANGELOG.md (~:115-116) says the numeric shift is "up to one ulp per component"** — the
   commit's own pinned fixture already moves 2 ulps in w, and a reviewer sweep (2000 arcs ×
   101 factors, identical operands on glibc vs libm paths) measured up to **4 ulps** (worst at
   ~1.1655 rad, t = 0.39). Restate the bound honestly (e.g. "a few ulps, measured ≤ 4") so
   migrating users don't size exact-comparison windows at 1 ulp.

**First action on resume:** fix both doc sites, run the full gate, commit as "Address Stage 3
review findings", append the outcome to the Stage 3 row. Then proceed with Stage 4.

(Both were resolved on resume by the "Address Stage 3 review findings" commit, which also fixed a
third site carrying the same wrong ulp figure — the `bits()` doc comment in
`src/geometry/quaternion/tests.rs`. Findings re-verified independently first: the v1.4.1 tree was
extracted and run in both feature modes, and the glibc-vs-libm sweep was reproduced. See the
Stage 3 row.)

## Interruption record — 2026-08-15 night, during Stage 5 review

Third deliberate clean termination by the maintainer. Working tree clean at `77c73a7`; Stages 1–5
are implemented, gated, and committed. Stage 4 (surface trims, `ff2bd6f` + review fix `3a2e1ee`)
and Stage 5 (full enforcement + invert-once rework, `77c73a7`) both landed this session.

Stage 5's **post-commit review was interrupted with no verdicts filed**: the adversarial reviewer
was mid-run exercising an out-of-crate probe project against the new public API (checking invalid
transforms are truly unrepresentable downstream) and re-running the no_std suite; the fidelity
reviewer was still working through the 27-file diff. Neither had reported findings at termination.

**First action on resume:** re-run the Stage 5 review of `77c73a7` from scratch (fidelity against
the record's Stage 5 + sub-decisions, adversarial against the AGENTS.md invariants — the largest
and most safety-relevant diff of the program; do not skip it), fix any real blockers with gate +
"Address Stage 5 review findings" commit, then proceed with Stage 6 (error overhaul) per the
protocol above.

(Done on resume. The re-run review filed two blockers, both real: `77c73a7` had deleted
`Buffer::insert`'s `validate()` on a premise the sub-decision's own acceptance criterion
falsifies, so a transform derived by `*` or by a lookup — the ordinary flatten-a-chain-and-
re-publish pattern — could be stored and served denormalized, and five shipping doc sites
asserted the guarantee the code had stopped providing. Resolved by the "Address Stage 5 review
findings" commit, which restores the one-line ingest check and corrects eight doc sites; findings
were reproduced first with an out-of-crate probe against both trees. See the Stage 5 row. **The
restored check is the one decision in this program taken against a written sub-decision** — it
deserves the maintainer's explicit yes or no before 2.0.0.)
